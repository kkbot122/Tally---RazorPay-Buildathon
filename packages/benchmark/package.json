{
  "name": "@tally/benchmark",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "generate:dev": "tsx scripts/generate-dev.ts",
    "generate:benchmark": "tsx scripts/generate-benchmark.ts",
    "generate:pilot": "tsx scripts/generate-pilot.ts"
  },
  "dependencies": {
    "@tally/contracts": "workspace:*",
    "@tally/reconciliation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "tsx": "^4.19.2"
  }
}
